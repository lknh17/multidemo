# p09 原理详解 - 评测体系

> 本文档详细讲解大模型评测的理论基础：评测方法学、主流 Benchmark 设计原理、自定义评测设计等。

---

## 1. 评测方法学概述

大模型评测是衡量模型能力的关键环节。一个完善的评测体系需要回答三个核心问题：

**评什么**: 知识、推理、代码、对话、安全等多个维度。单一指标无法全面反映模型能力，需要多维度综合评测。

**怎么评**: 选择题 logit 比较、开放式生成 + 答案提取、代码执行 + 测试用例等。不同任务需要不同评测方法。

**评多少**: 样本量需要足够大以保证统计显著性。经验法则：至少 100+ 样本，理想情况 1000+。

### 评测范式分类

| 范式 | 方法 | 代表 | 优点 | 缺点 |
|------|------|------|------|------|
| 选择题 | 比较选项 logit | MMLU/C-Eval | 客观、可复现 | 只考察识别能力 |
| 生成式 | 生成回答 + 提取答案 | GSM8K | 考察推理过程 | 答案提取可能出错 |
| 执行式 | 生成代码 + 运行测试 | HumanEval | 最接近实际 | 环境依赖复杂 |
| 人工评测 | 人类打分 | Chatbot Arena | 最真实 | 成本高、主观性强 |
| 模型评测 | 用强模型评分 | MT-Bench | 可扩展 | 依赖评分模型质量 |

### few-shot vs zero-shot

few-shot 评测在 prompt 中提供若干示例，帮助模型理解任务格式。研究表明：

- **基座模型**: few-shot 效果远好于 zero-shot（模型需要示例才能理解格式）
- **SFT 模型**: zero-shot 可能更好（模型已通过指令微调学会格式）
- **最佳实践**: 两种都测，取更好的分数

---

## 2. MMLU 评测详解

MMLU (Massive Multitask Language Understanding) 由 Dan Hendrycks 等人于 2021 年提出，是最广泛使用的英文知识评测基准。

### 数据集构成

- **57 个学科**: 涵盖 STEM、人文、社会科学、专业领域等
- **15,908 道题**: 每题四选一
- **难度梯度**: 从高中到专业水平

### 评测方法

标准方法是 **logit 比较法**：

```
给定 prompt = few-shot示例 + 当前问题
比较模型在最后一个位置输出 A/B/C/D 四个 token 的 logit
选 logit 最大的作为答案
```

$$P(answer=k) = \frac{e^{z_k}}{\sum_{j \in \{A,B,C,D\}} e^{z_j}}$$

其中 $z_k$ 是选项 k 对应 token 的 logit。

**为什么用 logit 而不是生成**: 生成式评测需要解析模型输出（可能格式不标准），logit 法更稳定可靠。

### 基座模型 vs SFT 模型的评测差异

基座模型用 few-shot prompt，SFT 模型可以用指令格式：

```
# SFT 模型的 prompt
<|im_start|>system
你是一个知识渊博的助手。<|im_end|>
<|im_start|>user
以下是一道关于计算机科学的选择题，请直接回答选项字母。
题目：...
A. ... B. ... C. ... D. ...<|im_end|>
<|im_start|>assistant
```

---

## 3. C-Eval 评测详解

C-Eval 是中文大模型评测基准，由上海交通大学等团队于 2023 年提出。

### 设计特点

- **52 个学科**: 涵盖中国特色学科（如近代史、中国语言文学）
- **13,948 道题**: 四选一
- **中文原生**: 非翻译，包含中国文化和教育体系知识

### 与 MMLU 的对比

| 维度 | MMLU | C-Eval |
|------|------|--------|
| 语言 | 英文 | 中文 |
| 学科 | 57 个（西方教育体系） | 52 个（中国教育体系） |
| 数据源 | 美国标准化考试 | 中国各级考试 |
| 评测方法 | 相同（logit 比较） | 相同 |

### 评测中文能力的重要性

对于面向中文用户的模型，C-Eval 比 MMLU 更能反映实际使用效果。继续预训练注入中文知识后，C-Eval 分数应有显著提升。

---

## 4. GSM8K 评测详解

GSM8K (Grade School Math 8K) 由 OpenAI 于 2021 年提出，评测数学推理能力。

### 数据集特点

- **8,792 道题**: 小学到初中水平的数学应用题
- **需要多步推理**: 平均每题需要 2-8 步计算
- **答案格式**: 推理过程 + `#### 最终数字`

### Chain-of-Thought (CoT)

GSM8K 的关键在于 CoT 推理：

```
问题：小明有 15 个苹果，给了小红 3 个，又买了 7 个，现在有多少？

思考过程：
1. 小明原来有 15 个苹果
2. 给了小红 3 个：15 - 3 = 12
3. 又买了 7 个：12 + 7 = 19
#### 19
```

**CoT 的关键作用**: 让模型"展示工作过程"，而非直接跳到答案。研究表明 CoT 可以显著提升数学推理准确率。

### 答案提取策略

从模型生成的文本中提取最终数字答案是评测的关键步骤：

1. 查找 `####` 标记后的数字（标准 GSM8K 格式）
2. 查找 "答案是" 后面的数字
3. 查找最后一个数学表达式的结果
4. 提取文本中最后出现的数字

### RL 训练对数学能力的提升

强化学习（特别是 GRPO）可以通过奖励函数直接优化答案正确性，对数学推理能力提升最为显著。

---

## 5. HumanEval 评测详解

HumanEval 由 OpenAI 于 2021 年提出，评测代码生成能力。

### 任务格式

给定函数签名和 docstring，生成完整的函数体：

```python
def has_close_elements(numbers: List[float], threshold: float) -> bool:
    """Check if in given list of numbers, are any two numbers
    closer to each other than given threshold.
    >>> has_close_elements([1.0, 2.0, 3.9, 4.0, 5.0, 2.2], 0.3)
    True
    """
    # 模型需要生成这部分代码
```

### pass@k 指标

$$\text{pass@k} = 1 - \frac{\binom{n-c}{k}}{\binom{n}{k}}$$

其中 $n$ 是生成的总样本数，$c$ 是通过测试的样本数，$k$ 是选取的样本数。

**直觉**: pass@1 = "一次就对的概率"；pass@10 = "十次里至少对一次的概率"。

### 安全执行

生成的代码需要在沙箱中执行，注意：
- 设置超时限制（防止无限循环）
- 在子进程中执行（防止影响主进程）
- 限制系统调用（防止恶意代码）

---

## 6. Benchmark 泄露问题

### 什么是 Benchmark 泄露

训练数据中包含评测 benchmark 的题目，导致模型"背答案"而非真正理解。这是当前评测体系面临的最严重问题之一。

### 检测方法

1. **N-gram 重叠检测**: 检查训练数据与评测集的文本重叠
2. **Canary String**: 在评测集中插入随机字符串，检查模型是否记住
3. **Performance Gap**: 对比公开 benchmark 和私有测试集的分数差异

### 应对策略

- 使用多个 benchmark 交叉验证
- 定期更新评测集
- 关注实际任务表现而非 benchmark 分数
- 使用 human evaluation 作为补充

---

## 7. 自定义评测设计

### 设计原则

1. **明确评测目标**: 每个 prompt 对应一个明确的能力维度
2. **多样化 prompt**: 避免格式单一导致的偏差
3. **可量化的评分标准**: 避免模糊的主观评价
4. **足够的样本量**: 每个维度至少 20+ 题

### 评分维度设计

| 维度 | 说明 | 评分方法 |
|------|------|----------|
| 准确性 | 回答是否正确 | 关键词匹配 / 答案提取 |
| 流畅性 | 语言是否通顺 | 重复率 / 困惑度 |
| 相关性 | 是否切题 | 语义相似度 |
| 安全性 | 是否拒绝危险请求 | 关键词检测 |
| 完整性 | 回答是否充分 | 长度 + 覆盖率 |

### 自动化 vs 人工评测

自动化评测（关键词匹配、代码执行）可扩展但粗糙；人工评测精确但成本高。最佳实践是**自动化筛选 + 人工抽检**。

---

## 8. 从评测到改进

评测不是目的，而是指导模型改进的手段。

### 评测驱动的改进策略

| 评测发现 | 改进方向 |
|----------|----------|
| 知识不足 | 继续预训练注入更多知识 |
| 对话能力差 | 增加高质量 SFT 数据 |
| 数学推理弱 | RL 训练 + 数学专项数据 |
| 代码能力差 | 代码数据 SFT + 执行反馈 RL |
| 安全性不足 | DPO/RLHF 对齐 + 安全数据 |

### 评测的迭代循环

```
训练 → 评测 → 分析短板 → 针对性改进 → 重新训练 → 重新评测
```

这个循环是大模型开发的核心工作流。评测体系的质量直接决定了改进的方向和效率。
